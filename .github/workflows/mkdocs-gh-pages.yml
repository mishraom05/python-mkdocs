name: Deploy MkDocs to GitHub Pages

on:
  push:
    branches:
      - main  

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build MkDocs site
        run: mkdocs build --config-file project-doc/mkdocs.yml
      
      - name: Configure git
        run: |
                git config --global user.name "github-actions[bot]"
                git config --global user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Set git remote with PAT
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN_CLASSIC }}
        run: git remote set-url origin https://x-access-token:${GH_TOKEN}@github.com/mishraom05/python-mkdocs.git

      - name: Deploy to GitHub Pages
        run: mkdocs gh-deploy --config-file project-doc/mkdocs.yml --force --remote-branch gh-pages --remote-name origin